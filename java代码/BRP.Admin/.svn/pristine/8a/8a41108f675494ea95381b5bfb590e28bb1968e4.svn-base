package BRP.model;
import BRP.data.ViewBookCompensationMaster;
public class ViewBookCompensationManager extends ViewBookCompensationMaster 
{
    private static ViewBookCompensationManager _instance;
    public  static ViewBookCompensationManager getInstance()    
    {
        if (_instance == null)         
        {
            _instance = new ViewBookCompensationManager();
        }
        return _instance;
    }
    private   ViewBookCompensationManager()    
    {
        this.setTypeName("ViewBookCompensation");
    }
}